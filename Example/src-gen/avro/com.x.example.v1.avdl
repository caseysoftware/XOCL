/**
 * This is a autogenerated using the com.x.ocl:xocl-maven-plugin:1.4.19 maven plugin.
 * The contents are generated from the following XOCL files:
 *            Example.Contracts.xocl
 *            Example.Version.xocl
 *            Example.Transactions.xocl
 *            Example.Processes.xocl
 *            Example.Messages.xocl
 * DO NOT EDIT THIS FILE DIRECTLY
 */
@namespace("com.x.example.v1")
@status("workingdraft")
protocol com_x_example_v1 {
	import idl "com.x.core.v1.avdl";

	record SearchOrder {
		union{null,long} itemsRequested = null;
 		union{null,long} startItemIndex = null;
  	}

	@topics("/com.x.example.v1/OrderShipment/InventoryUpdated")
	@version("1.2.4")
	record InventoryUpdated {
		string SKU;
 		int StockLevel = 0;
  	}

	@topics("/com.x.example.v1/OrderFulfillment.WithConfiguration/PrepareOrderSucceeded /com.x.example.v1/OrderFulfillment/PrepareOrderSucceeded /com.x.example.v1/OrderFulfillment.WithAuthorization/PrepareOrderSucceeded")
	@version("1.2.4")
	record PrepareOrderSucceeded {
		//A GUID that is 32-char hexidecimal string
		string orderID;
  	}

	record LookupOrder {
		//A GUID that is 32-char hexidecimal string
		string orderID;
  	}

	@topics("/com.x.example.v1/OrderShipment/ShipOrderSucceeded /com.x.example.v1/QuickStartSample/ShipOrderSucceeded /com.x.example.v1/OrderDropShipment/ShipOrderSucceeded")
	@version("1.2.4")
	record ShipOrderSucceeded {
		//A GUID that is 32-char hexidecimal string
		string orderID;
 		union{string, null} shippingService = null;
 		double estimatedWeight = 0.0;
 		string TrackingNumber;
  	}

	@topics("/com.x.example.v1/OrderFulfillment.WithConfiguration/PrepareOrder /com.x.example.v1/OrderFulfillment.WithAuthorization/PrepareOrder /com.x.example.v1/OrderFulfillment/PrepareOrder")
	@version("1.2.4")
	record PrepareOrder {
		//A GUID that is 32-char hexidecimal string
		string orderID;
  	}

	record LookupOrderFailed {
		//A GUID that is 32-char hexidecimal string
		string orderID;
 		array<com.x.core.v1.Error> errors;
  	}

	@topics("/com.x.example.v1/OrderShipment/UpdateInventoryFailure /com.x.example.v1/OrderDropShipment/UpdateInventoryFailure")
	@version("1.2.4")
	record UpdateInventoryFailure {
 	}

	record SearchOrderFailed {
		union{null,long} itemsRequested = null;
 		union{null,long} startItemIndex = null;
 		array<com.x.core.v1.Error> errors;
  	}

	@topics("/com.x.example.v1/OrderShipment/UpdateInventorySuccess /com.x.example.v1/OrderDropShipment/UpdateInventorySuccess")
	@version("1.2.4")
	record UpdateInventorySuccess {
 	}

	@topics("/com.x.example.v1/OrderFulfillment.WithConfiguration/OrderCancelled /com.x.example.v1/OrderFulfillment/OrderCancelled /com.x.example.v1/OrderFulfillment.WithAuthorization/OrderCancelled")
	@version("1.2.4")
	record OrderCancelled {
		//A GUID that is 32-char hexidecimal string
		string orderID;
  	}

	@topics("/com.x.example.v1/OrderFulfillment.WithAuthorization/PrepareOrderFailed /com.x.example.v1/OrderFulfillment/PrepareOrderFailed /com.x.example.v1/OrderFulfillment.WithConfiguration/PrepareOrderFailed")
	@version("1.2.4")
	record PrepareOrderFailed {
		//A GUID that is 32-char hexidecimal string
		string orderID;
  	}

	@topics("/com.x.example.v1/OrderShipment/ShipOrder /com.x.example.v1/QuickStartSample/ShipOrder /com.x.example.v1/OrderDropShipment/ShipOrder")
	@version("1.2.4")
	record ShipOrder {
		//A GUID that is 32-char hexidecimal string
		string orderID;
 		union{string, null} shippingService = null;
 		double estimatedWeight = 0.0;
  	}

	@topics("/com.x.example.v1/OrderFulfillment/OrderShipped /com.x.example.v1/OrderFulfillment.WithConfiguration/OrderShipped /com.x.example.v1/OrderFulfillment.WithAuthorization/OrderShipped")
	@version("1.2.4")
	record OrderShipped {
		//A GUID that is 32-char hexidecimal string
		string orderID;
  	}

	@topics("/com.x.example.v1/OrderShipment/UpdateInventory /com.x.example.v1/OrderDropShipment/UpdateInventory")
	@version("1.2.4")
	record UpdateInventory {
		string SKU;
 		int StockLevel = 0;
  	}

	enum OrderType {
		InHouse,
		OutSourced,
		DropShipped
	}

	record Order {
		//A GUID that is 32-char hexidecimal string
		string orderID;
 		OrderType orderType = "InHouse";
  	}

	record LookupOrderSucceeded {
		//A GUID that is 32-char hexidecimal string
		string orderID;
 		Order order;
  	}

	record SearchOrderSucceeded {
		union{null,long} itemsRequested = null;
 		union{null,long} startItemIndex = null;
 		array<Order> results;
  	}

	@topics("/com.x.example.v1/OrderFulfillment.WithAuthorization/OrderCreatedWithAuthorization")
	@version("1.2.4")
	record OrderCreatedWithAuthorization {
		Order order;
 		//A string representing the date and time as defined by RFC 3339's 'date-time' format with 'time-secfrac' defined.
		union{null,string} dateCreated = null;
 		bytes metadata;
  	}

	@topics("/com.x.example.v1/OrderFulfillment.WithConfiguration/OrderCreatedWithCreationMetadata")
	@version("1.2.4")
	record OrderCreatedWithCreationMetadata {
		Order order;
 		//A string representing the date and time as defined by RFC 3339's 'date-time' format with 'time-secfrac' defined.
		union{null,string} dateCreated = null;
 		bytes metadata;
  	}

	@topics("/com.x.example.v1/OrderFulfillment/OrderCreated /com.x.example.v1/QuickStartSample/OrderCreated")
	@version("1.2.4")
	record OrderCreated {
		Order order;
  	}

	@topics("/com.x.example.v1/OrderShipment/ShipOrderFailed /com.x.example.v1/QuickStartSample/ShipOrderFailed /com.x.example.v1/OrderDropShipment/ShipOrderFailed")
	@version("1.2.4")
	record ShipOrderFailed {
		//A GUID that is 32-char hexidecimal string
		string orderID;
 		union{string, null} shippingService = null;
 		double estimatedWeight = 0.0;
 		array<com.x.core.v1.Error> errors;
  	}


}